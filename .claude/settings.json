{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cat >/dev/null; touch \"$CLAUDE_PROJECT_DIR/.claude/.needs_validation\""
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "echo '✅ タスク完了チェック: output.md を output/<client>/<date>/<task>/ に作成したか確認してください（制御ブロック＋§1〜§5の統合1ファイル）。Local Second Brain syncは現在利用者の明示依頼、またはAccepted + 再利用価値 + Local root確認時だけKnowledge Curatorを起動してください。'"
          },
          {
            "type": "command",
            "command": "if [ -f \"$CLAUDE_PROJECT_DIR/.claude/.needs_validation\" ]; then python3 \"$CLAUDE_PROJECT_DIR/tools/validate_repository.py\"; STATUS=$?; if [ \"$STATUS\" -eq 0 ]; then rm -f \"$CLAUDE_PROJECT_DIR/.claude/.needs_validation\"; else echo '❌ validate_repository.py FAILED — shared contractまたはprivacy boundaryが壊れています。修正してから完了扱いにしてください。' >&2; exit \"$STATUS\"; fi; fi"
          }
        ]
      }
    ]
  }
}
